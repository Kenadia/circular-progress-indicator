{
  "name": "circular-progress-indicator",
  "version": "0.0.0",
  "private": true,
  "description": "An Angular directive that displays progress with a circular display",
  "author": "Ken Schiller <kenschiller@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Kenadia/circular-progress-indicator.git"
  },
  "scripts": {
    "start": "http-server -a 0.0.0.0 -p 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^0.7.4"
  }
}
